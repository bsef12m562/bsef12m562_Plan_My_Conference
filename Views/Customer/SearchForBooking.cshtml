@{
    ViewBag.Title = "SearchForBooking";
    Layout="~/Views/Shared/layoutscrollableone.cshtml";
}

           @using Conference.Models
@model List<Hotel>

@{ Customer c = (Customer)HttpContext.Current.Session["user"];}

<html>
<head><title>BookIt</title>
    <style>
    h3
    {
        color:yellow;
}
   </style>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
    
    <link href="~/Content/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="~/Content/sb-admin.css" rel="stylesheet">

    <!-- Custom Fonts -->
    <link href="font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

    
    
    
    </head>
    
<body>
   @*@*<h1>@c.Id</h1>
   
@foreach (var a in Model)
{
    <div class="container well-sm">

      <h3> Hotel Id: @a.Id</h3>
       
        <h3>@a.HotelName</h3>
        <br />

        <img src="~/Filesone/@a.PictureName" style="max-height:100%; max-width:100%"  />
        @Html.ActionLink("BookIt", "bookhotel", new { id = a.Id }, new { @class = "btn btn-success",Style="width:40%;"  })
        <br /><br>
    </div> }*@

    <div class="container">
     
      <!-- Modal -->
      <div class="modal fade" id="myModal" role="dialog">
        <div class="modal-dialog">

          <!-- Modal content-->
          <div class="modal-content">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal">&times;</button>
              <h4 class="modal-title">Modal Header</h4>
            </div>
            <div class="modal-body">
              <p>Some text in the modal.</p>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
          </div>

        </div>
      </div>

    </div> 



    <div class="wrapper" style="padding-top:10em; ">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>NAME </th>
                    <th>LOCATION</th>
                    <th>BUDGET</th>
                    <th>CAPACITY</th>
                    <th>STARTING DATE</th>
                    <th>ENDING DATE</th>
                    <th>TYPE</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var s in Model)
                            {
                    <tr>
                        <td>@s.Id</td>
                        <td>@s.HotelName</td>
                        <td>@s.Location</td>
                        <td>@s.Budget</td>
                        <td>@s.Capacity</td>
                        <td>@s.StartingAvailableDate</td>
                        <td>@s.EndingAvailableDate</td>
                        <td>@s.Type</td>


                        <td>@Html.ActionLink("Preview", "Preview", new { id = s.Id, sdate = @ViewBag.startingcustdate, edate = @ViewBag.endingcustdate }, new { @class = "btn btn-primary" })@Html.ActionLink("BookIt", "bookhotel", new { id = s.Id, sdate = @ViewBag.startingcustdate, edate = @ViewBag.endingcustdate }, new { @class = "btn btn-success" })</td>
                    </tr>
                            }                                 
                @* <button onclick="showDialogPreview(@s.Id)" class="btn btn-primary" data-toggle="modal" data-target="#myModal">Preview</button> *@
            </tbody>
        </table>
        <script>
           

            function showDialogPreview(x)
            {
               
                window.location.assign("/Customer/Preview?id=" + x);
               
            }

        </script>


    </div>
   
    <!-- /.container-fluid -->
    <!-- /#page-wrapper -->
    <!-- /#wrapper -->
    <!-- jQuery -->
    <script src="~/Scripts/jquery.js"></script>

    <!-- Bootstrap Core JavaScript -->
    <script src="~/Scripts/bootstrap.min.js"></script>


    </body>
    </html>

